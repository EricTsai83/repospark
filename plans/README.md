# Implementation Plans

Generated by the improve skill on 2026-07-02, against commit `3458fd0`.
Session ran non-interactively, so plans were written for the top findings by
leverage (the advisor's default). Execute in the order below unless
dependencies say otherwise. Each executor: read the plan fully before
starting, honor its STOP conditions, and update your row when done.

Verification baseline at planning time: `bun run lint` (tsc app + tsc convex +
ESLint `--max-warnings 0`) exit 0; `bun run test` 1715 tests — 4 UI-test
timeouts under heavy parallel machine load, all pass when re-run in isolation
(treat as environment flake, not signal).

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Enforce the cascade write budget in the repository artifact drain | P1 | M | — | DONE (reviewed & approved 2026-07-02; branch `advisor/001-cascade-write-budget-artifact-drain`, commit `703a3f7`, worktree `.claude/worktrees/agent-a70c2ac4043cd28e0` — awaiting operator merge) |
| 002  | Bound the `listViewStateByRepository` query | P2 | S | — | DONE (reviewed & approved 2026-07-02; branch `advisor/002-bound-artifact-view-state-query`, commit `d5912c1`, worktree `.claude/worktrees/agent-a1d3c34e1230c0f91` — awaiting operator merge) |
| 003  | Single-source the `constantTimeEqual` webhook-verification primitive | P2 | S | — | DONE (reviewed & approved 2026-07-02; branch `advisor/003-extract-constant-time-equal`, commit `89f43ac`, worktree `.claude/worktrees/agent-a6462610b33b89b94` — awaiting operator merge) |
| 004  | Add endpoint-level integration tests for the HTTP webhook routes | P2 | M | 003 (soft) | DONE (reviewed & approved 2026-07-02; branch `advisor/004-http-endpoint-integration-tests`, commit `f31de37`, worktree `.claude/worktrees/agent-a525c3ff9b53dd4b8` — awaiting operator merge. Full suite 1723/1723 advisor-verified. Note: callback tests exercise the `error=access_denied` path only; the OAuth success path needs GitHub API mocking and remains a known gap. Merge note: overlaps with 003 by one import hunk in `convex/http.ts` — whichever lands second needs a trivial rebase) |

Review evidence (2026-07-02, all verified by the advisor, not just executor
reports): full `bun run test` green in each worktree on a quiet machine
(001: 1717/1717, 002: 1717/1717, 003: 1720/1720); `bun run lint` exit 0
re-run by advisor in 001, executor-verified exit 0 in 002/003; scope diffs
exactly match each plan's in-scope list; new tests read and confirmed to
assert meaningful behavior (exact-count regression, budget early-stop with
zero-orphan convergence, fence overflow degradation, constant-time semantics).

| 005  | Give the heavy cascade-budget test an explicit 30s timeout (follow-up to 001: the test's ~11K-row fixture is load-bearing but intermittently exceeds vitest's 5s default in full-suite/cold-cache runs — flaked 3× today) | P1 | S | 001 | DONE (reviewed & approved 2026-07-02; merged fast-forward as `d1595da`) |
| 006  | Prune artifact version history to a retention cap (also fixes draft→version blob-ownership transfer at apply time — investigation found applied drafts keep referencing the version's HTML blob) | P2 | M | — | TODO |

## Merge record (2026-07-02)

All four plans were merged into `main` by the operator's instruction:
001 fast-forward to `703a3f7`, then merge commits `3fb86c7` (002), `c5dd8ca`
(003), `d6fb831` (004). Zero conflicts. Post-merge `bun run lint` +
`bun run test` run as the integration gate (see status below). Session
worktrees and merged branches were removed afterwards.

## Reconcile record — previous improve sessions (2026-07-02)

Operator declared all pre-existing advisor work stale and authorized cleanup.
Findings from the inspection before deletion:

- Old plan documents (`9590273`, `2d448f0`) are already in `main` history —
  the previous session's `plans/` directory was committed and later removed.
- Branches deleted as fully merged: `advisor/007-test-html-draft-repair-loop`,
  `advisor/008-index-find-artifact-in-folder-by-kind`,
  `advisor/009-truthful-html-validation-status`,
  `worktree-agent-aacad965cf28658ac`.
- Branches deleted as stale/unmerged (SHAs recorded for reflog recovery):
  `advisor/007-clear-copy-feedback-timeout`,
  `advisor/008-early-exit-find-artifact-in-folder`,
  `advisor/009-decode-html-entities-in-attribute-reads`,
  `advisor/010-restore-artifact-version` (all at `ee62ec6`, stale plan docs
  only, no source code); `advisor/006-bound-artifact-version-history`
  (`aa0f183`, retention-cap implementation);
  `worktree-agent-a363a7c418dc16742` (`e9b6bc2`, prune implementation).

**Still-live finding surfaced by reconcile**: `artifactVersions` history has
NO retention cap on `main` — `updateArtifactWrite` inserts a version row per
edit and nothing prunes them. Two prototype fixes existed on the deleted
branches (`aa0f183` retention cap; `e9b6bc2` prune + orphaned-HTML-blob
cleanup, which per its commit message was the later refinement). If this is
worth fixing, ask the advisor for a fresh plan (e.g. 005) rather than
resurrecting the deleted commits blindly.

## Dependency notes

- 004 lists 003 as a soft dependency only to avoid a merge overlap in
  `convex/http.ts` imports; they can land in either order if rebased.

## Findings considered and rejected

- **PERF: `listStaleJobsByStatusAndKind` scans thousands of rows before
  `.take()`** — rejected; the `withIndex(...).lt("leaseExpiresAt", now)` range
  plus `.take(limit)` reads at most `limit` rows (`convex/ops.ts:392-406`).
- **PERF: archived-thread scope listing is O(archived threads)** — rejected;
  `archivedThreadScopes` holds one row per (owner, repository scope), not per
  thread (`convex/chat/threads.ts:400-432`). Bounded by repo count.
- **PERF: `replaceChunksForArtifact` can hit the 16K write limit** — rejected;
  writes are bounded by `MAX_ARTIFACT_CHUNKS_PER_ARTIFACT = 200` per side
  (≤ ~401 writes/mutation), far under limits (`convex/artifactChunkStore.ts:57-111`).
- **PERF: `batchSetEmbeddings` sequential point reads** — not worth doing;
  ≤200 indexed point lookups inside one mutation, Convex mutations are
  single-threaded and local; no user-visible cost.
- **PERF: stream compaction threshold tuning (`MESSAGE_STREAM_COMPACT_CHUNK_THRESHOLD = 8`)** —
  not worth doing without measurements; speculative.
- **SEC: final assistant text persisted without `redact()`** — downgraded to
  by-design; every sandbox tool return path runs `redact()` before the model
  sees the output (`convex/chat/sandboxTools.ts:822,890,1032`), so the text
  stream cannot carry raw sandbox secrets. Optional defense-in-depth: also
  redact `finalDelta` at settlement — record here so it isn't re-audited as a
  vulnerability.
- **SEC: `requireOwnedDoc` doesn't check soft-delete state** — rejected as a
  finding; the layering (owner gate in `convex/lib/ownedDocs.ts`, state gates
  in `convex/lib/repositoryAccess.ts`) is deliberate and documented in the
  module docstrings.
- **SEC: sandbox tool deny-list is bypassable** — documented tradeoff in
  `docs/sandbox/sandbox-mode-security-system-design.md`; isolation is the
  load-bearing control, not the deny list.
- **SEC: share-token entropy** — no action; nanoid(40) ≈ 238 bits, well above
  need. A comment documenting the entropy target is welcome but not planned.
- **DX: missing `.env.example`** — rejected; the file exists and is
  extensively documented (frontend/backend split, where to set each var).
- **DEBT: `systemDesignPrompts.ts` is a god module** — not worth doing;
  612 lines of constant prompt templates is within this repo's normal file
  sizes and splitting adds indirection with no behavior gain.
- **TEST: 10 `*Node.ts` files untested** — deferred, not planned; `"use node"`
  actions are thin orchestration shells over `convex/lib/*` helpers that ARE
  tested (the repo's established pattern). Testing the shells needs heavier
  mocking than the value warrants today; revisit if a Node-action regression
  actually occurs.
- **SEC: webhook secret strength validation / returnTo query-param
  allowlist / shared-thread redaction metadata** — real but low-leverage
  hardening ideas; recorded here for a future pass rather than planned now.
